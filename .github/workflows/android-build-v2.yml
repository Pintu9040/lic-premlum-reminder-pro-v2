name: Android Build V2

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    name: Build & Validate Android APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: Make gradlew executable
        run: chmod +x gradlew

      - name: Validate google-services.json
        run: |
          echo "=== Validating Firebase Configuration ==="
          FILE=""
          if [ -f "app/google-services.json" ]; then
            FILE="app/google-services.json"
          elif [ -f "app/src/main/google-services.json" ]; then
            FILE="app/src/main/google-services.json"
          elif [ -f "google-services.json" ]; then
            FILE="google-services.json"
          fi

          if [ -z "$FILE" ]; then
            echo "::error::google-services.json was not found in app/, app/src/main/, or project root."
            exit 1
          fi

          echo "Found google-services.json at $FILE"

          python3 -c "
          import json, sys
          try:
              with open('$FILE', 'r') as f:
                  data = json.load(f)
              if 'project_info' not in data or 'client' not in data:
                  print('::error::google-services.json is missing required fields (project_info or client).')
                  sys.exit(1)
              client_list = data.get('client', [])
              if not client_list or not isinstance(client_list, list):
                  print('::error::google-services.json contains no client configurations.')
                  sys.exit(1)
              print('Firebase google-services.json is valid.')
          except Exception as e:
              print(f'::error::Failed to parse google-services.json: {e}')
              sys.exit(1)
          "

      - name: Build Debug APK
        run: ./gradlew assembleDebug --stacktrace

      - name: Upload Debug APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-debug-apk
          path: app/build/outputs/apk/debug/*.apk
          if-no-files-found: error
